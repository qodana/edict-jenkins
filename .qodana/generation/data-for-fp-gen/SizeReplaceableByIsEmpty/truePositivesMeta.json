[
    {
        "message": "'tokens.size() > 0' can be replaced with '!tokens.isEmpty()'",
        "contextSnippet": "                    // If relative path, not collapsible so leave as-is\n                    tokens.remove(0);\n                    if (tokens.size() > 0) token += tokens.remove(0);\n                    if (!isAbsolute) buf.append(token);\n                } else {",
        "equalIndicatorV2": "3ed080410f4db503"
    },
    {
        "message": "'lastVersion.length() > 0' can be replaced with '!lastVersion.isEmpty()'",
        "contextSnippet": "                try {\n                    String lastVersion = XMLUtils.getValue(\"/hudson/version\", configFile);\n                    if (lastVersion.length() > 0) {\n                        LOGGER.log(Level.FINE, \"discovered serialized lastVersion {0}\", lastVersion);\n                        return lastVersion;",
        "equalIndicatorV2": "06546c77be12d9c1"
    },
    {
        "message": "'tokens.size() > 0' can be replaced with '!tokens.isEmpty()'",
        "contextSnippet": "                    for (int j = 0; j < 3; j++) tokens.remove(i);\n                    if (i > 0) tokens.remove(i - 1);\n                    else if (tokens.size() > 0) tokens.remove(0);\n                }\n            } else",
        "equalIndicatorV2": "1d65aa1940654807"
    },
    {
        "message": "'decisionProviders.size() > 0' can be replaced with '!decisionProviders.isEmpty()'",
        "contextSnippet": "        // check whitelist\n        ExtensionList<RoutingDecisionProvider> decisionProviders = ExtensionList.lookup(RoutingDecisionProvider.class);\n        if (decisionProviders.size() > 0) {\n            for (RoutingDecisionProvider provider : decisionProviders) {\n                RoutingDecisionProvider.Decision fieldDecision = provider.decide(signature);",
        "equalIndicatorV2": "53bc1d00bdacc17d"
    },
    {
        "message": "'mask.length() > 0' can be replaced with '!mask.isEmpty()'",
        "contextSnippet": "     */\n    public boolean hasMaskedArguments() {\n        return mask.length() > 0;\n    }\n",
        "equalIndicatorV2": "a6ea4f4ef3f9c022"
    },
    {
        "message": "'port.length() > 0' can be replaced with '!port.isEmpty()'",
        "contextSnippet": "            }\n            String port = authorityMatcher.group(PARSE_AUTHORITY_PORT);\n            if (port != null && port.length() > 0) {\n                try {\n                    int iPort = Integer.parseInt(port);",
        "equalIndicatorV2": "67606e138a7f32a4"
    },
    {
        "message": "'value.length() > 0' can be replaced with '!value.isEmpty()'",
        "contextSnippet": "\n        public FormValidation doCheckCommand(@QueryParameter String value) {\n            if (value.length() > 0) {\n                return FormValidation.ok();\n            } else {",
        "equalIndicatorV2": "05742eeba44a1cb3"
    },
    {
        "message": "'body.length() > 0' can be replaced with '!body.isEmpty()'",
        "contextSnippet": "     */\n    public String prepend(String prefix, String body) {\n        if (body != null && body.length() > 0)\n            return prefix + body;\n        return body;",
        "equalIndicatorV2": "cc2d258a38e3ef2f"
    },
    {
        "message": "'req.getRestOfPath().length() == 0' can be replaced with 'req.getRestOfPath().isEmpty()'",
        "contextSnippet": "    public Object getTarget() {\n        StaplerRequest req = Stapler.getCurrentRequest();\n        if (req.getRestOfPath().length() == 0 && \"POST\".equals(req.getMethod())) {\n            // CLI connection request\n            if (\"false\".equals(req.getParameter(\"remoting\"))) {",
        "equalIndicatorV2": "d199f2257c48e94a"
    },
    {
        "message": "'ProcessKillingVeto.all().size() > 0' can be replaced with '!ProcessKillingVeto.all().isEmpty()'",
        "contextSnippet": "        @Override\n        public Boolean call() throws IOException {\n            return ProcessKillingVeto.all().size() > 0;\n        }\n    }",
        "equalIndicatorV2": "c893a1e4bd1a0bde"
    },
    {
        "message": "'tokens.size() > 0' can be replaced with '!tokens.isEmpty()'",
        "contextSnippet": "            if (token.equals(\".\")) {\n                tokens.remove(i);\n                if (tokens.size() > 0)\n                    tokens.remove(i > 0 ? i - 1 : i);\n            } else if (token.equals(\"..\")) {",
        "equalIndicatorV2": "7021253f27e0aa2c"
    },
    {
        "message": "'p.getUpstreamProjects().size() == 0' can be replaced with 'p.getUpstreamProjects().isEmpty()'",
        "contextSnippet": "            LOGGER.fine(\"assembling top level projects\");\n            for (AbstractProject p : Jenkins.get().allItems(AbstractProject.class))\n                if (p.getUpstreamProjects().size() == 0) {\n                    LOGGER.fine(\"adding top level project \" + p.getName());\n                    topLevelProjects.add(p);",
        "equalIndicatorV2": "cfcf197f0e93e626"
    },
    {
        "message": "'si.password1.length() != 0' can be replaced with '!si.password1.isEmpty()'",
        "contextSnippet": "        }\n\n        if (!(si.password1 != null && si.password1.length() != 0)) {\n            si.errors.put(\"password1\", Messages.HudsonPrivateSecurityRealm_CreateAccount_PasswordRequired());\n        }",
        "equalIndicatorV2": "41db5b2c3b0b5fcb"
    },
    {
        "message": "'decision.size() > 0' can be replaced with '!decision.isEmpty()'",
        "contextSnippet": "        // check whitelist\n        ExtensionList<RoutingDecisionProvider> decision = ExtensionList.lookup(RoutingDecisionProvider.class);\n        if (decision.size() > 0) {\n            for (RoutingDecisionProvider provider : decision) {\n                RoutingDecisionProvider.Decision methodDecision = provider.decide(signature);",
        "equalIndicatorV2": "37d27d6e074a42d3"
    },
    {
        "message": "'oneResult.getDependentsDisableStatus().size() > 0' can be replaced with '!oneResult.getDependentsDisableStatus().isEmpty()'",
        "contextSnippet": "        }\n\n        if (oneResult.getDependentsDisableStatus().size() > 0) {\n            for (PluginWrapper.PluginDisableResult oneDependentResult : oneResult.getDependentsDisableStatus()) {\n                if (restartIfNecessary(oneDependentResult)) {",
        "equalIndicatorV2": "db952ac2fc578abf"
    },
    {
        "message": "'value.length() > 0' can be replaced with '!value.isEmpty()'",
        "contextSnippet": "\n        public FormValidation doCheckToolHome(@QueryParameter String value) {\n            if (value.length() > 0) {\n                return FormValidation.ok();\n            } else {",
        "equalIndicatorV2": "fb3976b9d9493bae"
    },
    {
        "message": "'oneResult.getDependentsDisableStatus().size() > 0' can be replaced with '!oneResult.getDependentsDisableStatus().isEmpty()'",
        "contextSnippet": "\n        printIndented(indent, Messages.DisablePluginCommand_StatusMessage(oneResult.getPlugin(), oneResult.getStatus(), oneResult.getMessage()));\n        if (oneResult.getDependentsDisableStatus().size() > 0) {\n            indent += INDENT_SPACE;\n            for (PluginWrapper.PluginDisableResult oneDependentResult : oneResult.getDependentsDisableStatus()) {",
        "equalIndicatorV2": "e900cec1f49d9ab6"
    },
    {
        "message": "'jsonList.size() == 0' can be replaced with 'jsonList.isEmpty()'",
        "contextSnippet": "                jsonList.add(o);\n            }\n            if (jsonList.size() == 0 && toolInstallerMetadataExists) {\n                return FormValidation.warning(\"None of the tool installer metadata passed the signature check\");\n            } else if (!toolInstallerMetadataExists) {",
        "equalIndicatorV2": "3e94e604eed77502"
    },
    {
        "message": "'json.length() > 0' can be replaced with '!json.isEmpty()'",
        "contextSnippet": "    private boolean needToAddToCurrentView(StaplerRequest req) throws ServletException {\n        String json = req.getParameter(\"json\");\n        if (json != null && json.length() > 0) {\n            // Submitted via UI\n            JSONObject form = req.getSubmittedForm();",
        "equalIndicatorV2": "4bd8dfbbc9ea85f4"
    },
    {
        "message": "'req.getContextPath().length() > 0' can be replaced with '!req.getContextPath().isEmpty()'",
        "contextSnippet": "        SecurityContextHolder.clearContext();\n\n        String contextPath = req.getContextPath().length() > 0 ? req.getContextPath() : \"/\";\n        resetRememberMeCookie(req, rsp, contextPath);\n        clearStaleSessionCookies(req, rsp, contextPath);",
        "equalIndicatorV2": "1886ca297728e41c"
    },
    {
        "message": "'targets.length() != 0' can be replaced with '!targets.isEmpty()'",
        "contextSnippet": "            Map<String, String> record = new HashMap<>();\n\n            if (targets.length() != 0) {\n                String expandedTargets = targets;\n                if (build instanceof AbstractBuild) { // no expansion for pipelines",
        "equalIndicatorV2": "3e7bc2d4aed086ed"
    },
    {
        "message": "'labels.length() > 0' can be replaced with '!labels.isEmpty()'",
        "contextSnippet": "        final Set<LabelAtom> r = new TreeSet<>();\n        labels = fixNull(labels);\n        if (labels.length() > 0) {\n            Jenkins j = Jenkins.get();\n            LabelAtom labelAtom = j.tryGetLabelAtom(labels);",
        "equalIndicatorV2": "e78e7047fde3af7b"
    },
    {
        "message": "'ancestors.size() == 0' can be replaced with 'ancestors.isEmpty()'",
        "contextSnippet": "        List<Ancestor> ancestors = req.getAncestors();\n\n        if (ancestors == null || ancestors.size() == 0) {\n            // ???\n            return null;",
        "equalIndicatorV2": "59e36a60e779428e"
    },
    {
        "message": "'whitelistProviders.size() > 0' can be replaced with '!whitelistProviders.isEmpty()'",
        "contextSnippet": "        // check whitelist\n        ExtensionList<RoutingDecisionProvider> whitelistProviders = ExtensionList.lookup(RoutingDecisionProvider.class);\n        if (whitelistProviders.size() > 0) {\n            for (RoutingDecisionProvider provider : whitelistProviders) {\n                RoutingDecisionProvider.Decision methodDecision = provider.decide(signature);",
        "equalIndicatorV2": "89398386959f02b6"
    },
    {
        "message": "'trimmedToken.length() > 0' can be replaced with '!trimmedToken.isEmpty()'",
        "contextSnippet": "        for (String classNameTok : classNameTokA) {\n            String trimmedToken = classNameTok.trim();\n            if (trimmedToken.length() > 0) {\n                classNameTokL.add(trimmedToken);\n            }",
        "equalIndicatorV2": "e917d13c36260a5e"
    },
    {
        "message": "'kind.trim().length() > 0' can be replaced with '!kind.trim().isEmpty()'",
        "contextSnippet": "            if (null != parent) {\n                String kind = parent.getKind();\n                if (null != kind && kind.trim().length() > 0) scm = kind;\n            }\n            throw new UnsupportedOperationException(\"getAffectedFiles() is not implemented by \" + scm);",
        "equalIndicatorV2": "88177e809d01a6f2"
    },
    {
        "message": "'getWarnings().size() > 0' can be replaced with '!getWarnings().isEmpty()'",
        "contextSnippet": "        @Restricted(DoNotUse.class)\n        public boolean hasWarnings() {\n            return getWarnings().size() > 0;\n        }\n",
        "equalIndicatorV2": "20d8b8c73eeb070a"
    },
    {
        "message": "'runs.size() > 0' can be replaced with '!runs.isEmpty()'",
        "contextSnippet": "        HistoryPageEntry<Run> entry = new HistoryPageEntry<>(run);\n        // Assert that runs have been added in descending order\n        if (runs.size() > 0) {\n            if (entry.getEntryId() > runs.get(runs.size() - 1).getEntryId()) {\n                throw new IllegalStateException(\"Runs were out of order\");",
        "equalIndicatorV2": "2bb97c5b033a10a2"
    },
    {
        "message": "'c.getName().length() > 0' can be replaced with '!c.getName().isEmpty()'",
        "contextSnippet": "        void broadcast() {\n            for (Computer c : Jenkins.get().getComputers()) {\n                if (c.getName().length() > 0) { // i.e. not master\n                    VirtualChannel ch = c.getChannel();\n                    if (ch != null) {",
        "equalIndicatorV2": "055ee510c716d910"
    },
    {
        "message": "'extra.trim().length() > 0' can be replaced with '!extra.trim().isEmpty()'",
        "contextSnippet": "\n        String extra = authorityMatcher.group(PARSE_AUTHORITY_EXTRA);\n        if (extra != null && extra.trim().length() > 0) {\n            return false;\n        }",
        "equalIndicatorV2": "caa78f5be6bf89af"
    },
    {
        "message": "'Jenkins.RESOURCE_PATH.length() > 0' can be replaced with '!Jenkins.RESOURCE_PATH.isEmpty()'",
        "contextSnippet": "                return IconSet.toNormalizedIconNameClass(path);\n            }\n            if (Jenkins.RESOURCE_PATH.length() > 0 && path.startsWith(Jenkins.RESOURCE_PATH)) {\n                // will to live falling\n                path = path.substring(Jenkins.RESOURCE_PATH.length());",
        "equalIndicatorV2": "22ffbee120823027"
    },
    {
        "message": "'label.length() > 0' can be replaced with '!label.isEmpty()'",
        "contextSnippet": "\n    /* package */TreeString(final TreeString parent, final String label) {\n        assert parent == null || label.length() > 0; // if there's a parent,\n                                                     // label can't be empty.\n",
        "equalIndicatorV2": "2c588fd5e0bdbddd"
    },
    {
        "message": "'crumb.length() > 0' can be replaced with '!crumb.isEmpty()'",
        "contextSnippet": "            crumb = issueCrumb(request, getDescriptor().getCrumbSalt());\n            if (request != null) {\n                if (crumb != null && crumb.length() > 0) {\n                    request.setAttribute(CRUMB_ATTRIBUTE, crumb);\n                } else {",
        "equalIndicatorV2": "a325d2ba5698e89e"
    },
    {
        "message": "'rest.length() > 0' can be replaced with '!rest.isEmpty()'",
        "contextSnippet": "\n            List<List<Path>> glob = null;\n            boolean patternUsed = rest.length() > 0;\n            boolean containsSymlink = false;\n            boolean containsTmpDir = false;",
        "equalIndicatorV2": "5de0c1237c5c15b6"
    },
    {
        "message": "'c.getName().length() == 0' can be replaced with 'c.getName().isEmpty()'",
        "contextSnippet": "        });\n        for (Computer c : Jenkins.get().getComputers()) {\n            if (c.getName().length() == 0) {\n                continue; // master\n            }",
        "equalIndicatorV2": "c024ac993f9fc617"
    }
]